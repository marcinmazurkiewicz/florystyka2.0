application:
  security:
    allowedOrigins:
      - http://localhost:8080
      - https://localhost:8080
    allowedMethods:
      - GET
    allowedHeaders: '*'
quizer:
  quizQuestionNumber: 3
  resourcesFolder: ~/resources/florystyka
  questionsImgFolder: /questions_images
  pdfName: exam.pdf
  quizTimeInSeconds: 3600
  quizPassThreshold: 70
spring:
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:/db/changelog.xml
    enabled: true
